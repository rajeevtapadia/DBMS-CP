<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h2>
      <table>
        <thead>
          <tr>
            <th>ID</th>
            <th>Song Title</th>
            <th>Duration</th>
            <th>Artist Name</th>
            <th>Album Title</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#each songs}}
            <tr>
              <td>{{this.id}}</td>
              <td>{{this.songTitle}}</td>
              <td>{{this.duration}}</td>
              <td>{{this.name}}</td>
              <td>{{this.albumTitle}}</td>
              <td>
                <form
                  action="/playlists/{{../userId}}/deletesong/{{this.playlist_id}}/{{this.song_id}}"
                  method="POST"
                  style="display: inline-block;"
                >
                  {{#if csrfToken}}
                    <input type="hidden" name="_csrf" value="{{csrfToken}}" />
                  {{/if}}
                  <button type="submit">Delete</button>
                </form>
              </td>
            </tr>
          {{/each}}
        </tbody>
      </table>

    </h2>

    <h2>Add Song to Playlist</h2>
    <form action="/playlists/{{userId}}/add/{{playlistId}}" method="POST">
      {{#if csrfToken}}
        <input type="hidden" name="_csrf" value="{{csrfToken}}" />
      {{/if}}

      <label for="songId">Song ID:</label>
      <input type="number" id="songId" name="songId" required />
      <br />

      <button type="submit">Add Song</button>
    </form>

  </body>
</html>